# QuickTool v1.7.1

Windows 轻量级桌面效率工具集：划词翻译 · 截图翻译 · 截图对照 · 置顶便签 · RAG 快捷问答

## 🆕 v1.7.1 核心更新（围绕"代码类资料 RAG 质量"实测打磨）

### 代码块原子化切分（fence-aware splitting）
- 针对"讲解算法+参考代码"类 md 实测（语料 1552 文件 / 13728 代码块：84% ≤500 字符本已安全、16% 被按标点腰斩且中间片段丢失 ``` 标记）
- fence 块独立成原子单元：**≤3000 字符（实测覆盖 p99=3045 全部教学代码）永不切**，放不下就独占 chunk——**检索命中即完整代码**
- \>3000 字符整块跳过不入库（实测该尺寸几乎全是 Coze/Dify 数据 blob：单行 3 万字符的 JSON 测试样例，检索命中不了、命中了也没用）

### 生成上下文防污染（8B 级小模型友好）
- 生成预算 4000 → **6000** 字符（3000 字符代码块命中不再挤掉全部来源）
- **同节去重**：同文档同标题链的命中只留最高分一块，防单章节连续切片霸榜
- **代码配额 = 1**：含代码块的上下文最多带 1 块，防小模型面对大段代码的注意力稀释/复读倾向
- 系统提示词加约束："代码是参考实现，回答以原理讲解为主"

### 导入即预向量化（批量导入体验修复）
- 原设计"首问才补算向量"在批量导入（千级文档 ≈ 数万 chunk）下首问补算十几分钟无进度
- 改为：**导入完成自动后台预向量化**，UI 实时「后台向量化 d/t」；未配 Key 静默跳过，ask 时 gate 兜底
- `embed`/`rerank` 遇 **429/5xx/网络错误指数退避重试**（最多 3 次），批量场景不再一碰限流整批失败

### 批量导入
- 文档库新增「导入文件夹…」：递归扫描 .txt/.md/.pdf（隐藏目录/文件跳过、扩展名大小写不敏感）
- 批量 PDF 信号量限流并发提取 2 个，聚合进度「PDF x/y：」

### 回答更详细
- 回答长度 300 字内 → **500~900 字**（`max_tokens` 800→1500），把机制和细节讲透

## ✅ 验证
- smoke **271/271**（fence 原子化 5 项 + 上下文防污染 5 项 + 预向量化/重试 7 项 + 批量导入 9 项）
- e2e 真按键全链路 **14/14**（--exe 打包版）
- 溯源审计：DCL/volatile 实测问答，答案逐句可溯源至 `toBeBetterJavaer` 原文

## 📦 使用
下载 `QuickTool_v1.7.1.exe` 直接运行（单文件、免安装）。RAG 问答需在设置页 → 凭据中心填 API Key（本机 DPAPI 加密存储）；文档库支持 txt/md/pdf 批量导入，导入后自动后台向量化。

---
*v1.7.0（RAG 快捷问答 + 凭据中心 + PDF 导入）内容见上一版 Release · 运行时零第三方依赖（唯 pypdf 例外）*
