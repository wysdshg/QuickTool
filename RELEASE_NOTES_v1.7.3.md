# QuickTool v1.7.3

> 下载 [QuickTool_v1.7.3.exe](./QuickTool_v1.7.3.exe) 单文件即可使用（绿色免安装，Win10/11）。
> 详细技术说明见 [README](./README.md) 第四节与版本记录。

## 🆕 v1.7.3 更新内容

### 全局热键收敛：7 个 → 3 个
- **保留** `Ctrl+Q` 划词翻译直达（最高频，一键不动）
- **新增** `Ctrl+Alt+Q` **文字动作菜单**：抓取选中的文字，就近弹出 `1 翻译 / 2 便签 / 3 问答`，按数字键或点击即执行——抓到的文字直接透传，不用二次抓词
- **新增** `Ctrl+Alt+W` **截图动作菜单**：框选屏幕区域后先看到截了什么，再选 `1 对照 / 2 OCR`
- 移除的 6 个热键（设置 / 退出 / 截图OCR / 截图对照 / 便签 / RAG）功能全部还在：**托盘菜单**（右键）与**动作菜单**均可触达；设置可双击托盘打开
- 好处：注册面从 42 个候选组合缩到约 12 个，与输入法/网盘/IDE 的热键冲突概率断崖式下降；需要记的组合只剩 3 个

### 迷你按钮升级：「译 / 便 / 问」三圆 + 透明背景
- 拖选文字后的迷你按钮从两圆扩为**三圆**：新增「问」——选中文字直接作为背景打开 RAG 问答窗（复用拖选时已抓好的文字，零等待）
- **色键透明**：圆外完全透明且点击穿透，点两圆之间的缝隙会直接落到下层应用，没有死区

### 其他
- 截图管线重构：托盘直达与截图菜单共用同一条「抓屏 → 消费」管线（`_pin_from_data` / `_ocr_from_data`）
- 验证：smoke **285/285**、e2e **14/14**（源码 + 打包双跑）

---

**升级提示**：覆盖旧 exe 即可，配置兼容；旧版本的自定义热键（`hotkey_ocr` 等）不再读取，如需找回旧键位可在设置里把三个热键改成任意组合。
