# QuickTool v1.7.2

> 下载 [QuickTool_v1.7.2.exe](./QuickTool_v1.7.2.exe) 单文件即可使用（绿色免安装，Win10/11）。
> 详细技术说明见 [README](./README.md) 第 4.13 节与版本记录。

## 🆕 v1.7.2 更新内容

### 生成模型切换增强（凭据中心）
- **模型历史记忆**：每个厂商自动记住你用过的模型（去重、最近在前、最多 8 个），换过一次下次下拉直选，不再手打模型全名
- **「模型覆盖」输入框 → 「生成模型」下拉框**：切换生成服务商时，下拉列表实时跟随新厂商的历史模型；跨厂商残留的模型名自动回退为新厂商默认
- **检索参数进设置页**：RAG 区新增「检索变体数」（1~5，选 1 可关 Fusion 省一次调用）、「每路召回 TopK」（10~100）、「精排 TopK」（1~30），多概念问题加大召回不再要改配置文件
- **实测推荐组合**：精排 TopK=10 + 上下文预算 8000，枚举类知识（锁升级全链、GC Roots 六类）稳定进上下文，两档模型答案完整度显著提升
- **魔搭默认模型更新**为 Qwen/Qwen3.8-Flash-Next

### 切换模型免重启（v1.7.0 遗留 bug 修复）
- 之前在设置里切换生成厂商/模型后，RAG 问答仍发旧厂商（RagApi 端点在会话内被缓存），需要重启程序才生效
- 现在**保存设置即生效**，下一条问答走新厂商；RagApi 构建时新增端点留痕日志（`RAG-API chat=模型@厂商 …`），账单对不上一眼可查

### 设置页滚轮防误改
- 之前滚轮滚动设置页时，鼠标扫过任何下拉框会直接改掉选中项（比如生成服务商从智谱滚成 DeepSeek）
- 现在悬停在下拉框上滚轮只滚页面、不改值

### Office 拖选出「c」修复
- 之前在 Word 里拖选文字会莫名把所选内容清除、原地多出一个字母 c（模拟 Ctrl+C 的四个键盘事件零间隔注入，Word 的异步输入管线丢修饰键，把组合键当成了裸字符键入）
- 改为分步注入并留键间隔，Word 真机验证：拖选抓词成功、文字完好

### 其他
- RAG 生成上下文预算 6000→8000 字符（配合精排 TopK=10）
- 凭据中心「自定义 Key」行标签截断修复
- smoke 272/272、e2e 14/14（源码 + 打包双跑）

---

**升级提示**：覆盖旧 exe 即可，配置兼容；API Key 经 DPAPI 加密绑定原机器，换电脑需重新填写。
